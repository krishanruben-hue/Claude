{
  "name": "pokegrade",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd pokegrade/frontend && npm install && npm run build",
    "start": "cd pokegrade/backend && npm install && node index.js",
    "dev": "cd pokegrade/frontend && npm install && npm run dev"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
